# Claude repository instructions

@AGENTS.md

This file intentionally imports the root agent instructions so Claude uses the
same product, data-safety, verification, and release constraints as every other
repository agent.
